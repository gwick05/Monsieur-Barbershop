function SearchFilterSortSection({ children }) {
  return (
    <div className="flex h-10 items-center justify-between px-4">
      {children}
    </div>
  );
}

export default SearchFilterSortSection;
